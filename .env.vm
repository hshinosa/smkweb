APP_NAME="SMAN 1 Baleendah"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://sman1baleendah.sch.id

APP_LOCALE=id
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=id_ID

APP_MAINTENANCE_DRIVER=file

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=daily
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=warning
LOG_DAILY_DAYS=14

# Database - Docker PostgreSQL
DB_CONNECTION=pgsql
DB_HOST=db
DB_PORT=5432
DB_DATABASE=sman1_baleendah
DB_USERNAME=sman1_user
DB_PASSWORD=sman1_password_2024
DB_SSLMODE=disable

# Session - Use redis for performance
SESSION_DRIVER=redis
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=public
QUEUE_CONNECTION=database

# Cache - Use redis
CACHE_STORE=redis
CACHE_PREFIX=sman1_baleendah_

MEMCACHED_HOST=127.0.0.1

# Redis - Docker service
REDIS_CLIENT=phpredis
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379
REDIS_DB=0
REDIS_CACHE_DB=1
REDIS_PREFIX=sman1_baleendah_

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="info@smansa.hshinoshowcase.site"
MAIL_FROM_NAME="${APP_NAME}"

VITE_APP_NAME="${APP_NAME}"

# CDN Configuration
ASSET_URL="${APP_URL}"
CDN_URL="${APP_URL}"

# Qdrant - Docker service
QDRANT_HOST=qdrant
QDRANT_PORT=6333
QDRANT_API_KEY=

# AI Configuration (Groq - managed in Admin > AI Settings)
GROQ_API_KEY=

# Self-hosted Embedding Service (Docker TEI)
EMBEDDING_PROVIDER=tei
EMBEDDING_BASE_URL=http://embedding:8080
EMBEDDING_MODEL=intfloat/multilingual-e5-small
EMBEDDING_DIMENSIONS=384
