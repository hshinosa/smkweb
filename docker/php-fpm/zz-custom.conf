[global]
; Override error_log to use stdout directly
error_log = /dev/stderr

[www]
; Don't change user/group, let entrypoint handle it or run as root
; user = www-data
; group = www-data

; Ensure logs work
access.log = /dev/stdout
catch_workers_output = yes
decorate_workers_output = no
